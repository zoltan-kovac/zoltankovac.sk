import { Box, Typography } from "@mui/joy";
import * as React from "react";

type SkillsProps = {};

export const Skills: React.FC<SkillsProps> = (): JSX.Element => {
  return (
    <Box>
      <Typography level="body-lg">Typescript</Typography>
    </Box>
  );
};
